This folder groups the generated skill folders so VS Code stays cleaner.

These entries are links to the real locations at the workspace root.
The root folders are still there because Codex and the skills tooling expect them.
